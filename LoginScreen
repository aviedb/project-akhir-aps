class LoginScreen
  Declarations
    LoginController controller
    String email
    String password

  void setController(LoginController controller)
  begin
    this.controller = controller
  end

  void submitLogin()
  begin
    this.controller.login(this.email, this.password)
  end

  void tampilPesanError()
  begin
    menampilkan pesan error
  end
